---
phase: 04-red-team-audit
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - .planning/phases/04-red-team-audit/AUDIT-REPORT.md
autonomous: true
requirements: [CMD-03, DEP-01, DEP-02]

must_haves:
  truths:
    - "Every std::process::Command and tokio::process::Command call is catalogued with file, line, argument method, and risk level"
    - "All cargo audit findings are documented with severity and remediation path"
    - "All cargo deny findings are documented with category and remediation path"
    - "All four attack surfaces (command injection, signal/PID, LLM trust, path traversal) have specific findings"
    - "Each finding has an ID, severity rating, location, description, exploit scenario, and remediation recommendation"
  artifacts:
    - path: ".planning/phases/04-red-team-audit/AUDIT-REPORT.md"
      provides: "Comprehensive security audit report with all findings"
      contains: "FINDING-"
  key_links:
    - from: ".planning/phases/04-red-team-audit/AUDIT-REPORT.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Requirement ID references in findings"
      pattern: "CMD-|DEP-|SIG-|LLM-|PATH-"
---

<objective>
Compile the comprehensive AUDIT-REPORT.md from research findings and dependency tool output.

Purpose: This is the primary deliverable of Phase 4 -- a structured vulnerability inventory that becomes the input for Phase 5 (Purple Team hardening). It must cover all four attack surfaces with specific, actionable findings.
Output: AUDIT-REPORT.md with numbered findings, severity ratings, and remediation recommendations
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-red-team-audit/04-RESEARCH.md
@.planning/phases/04-red-team-audit/04-01-SUMMARY.md
@.planning/phases/04-red-team-audit/cargo-audit-output.txt
@.planning/phases/04-red-team-audit/cargo-deny-output.txt
@src/main.rs
@src/signal.rs
@src/grouper/llm.rs
@src/grouper/mod.rs
@src/diff/parser.rs
@src/cache.rs
@src/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify research findings against source code</name>
  <files>.planning/phases/04-red-team-audit/AUDIT-REPORT.md</files>
  <action>
Before writing the report, verify every finding from 04-RESEARCH.md against the actual source code. The research identified:

1. **Command::new calls (CMD-03):** Grep `src/` for all `Command::new` calls. Verify the 5 call sites listed in research are still accurate (file, line number, argument method). Check for any NEW Command::new calls added since research was done.
   - `grep -rn "Command::new" src/`

2. **Signal/PID handling:** Read `src/signal.rs` fully. Confirm PID file path, write method, read method, and cleanup behavior match research findings.

3. **LLM output trust:** Read `src/grouper/llm.rs` and `src/grouper/mod.rs`. Confirm unbounded deserialization, missing size limits, and field validation gaps.

4. **Path traversal:** Read `src/diff/parser.rs`, `src/cache.rs`, `src/config.rs`. Confirm path handling matches research findings.

5. **Additional findings:** Check for the `/tmp/semantic-diff.log` in main.rs, the UTF-8 truncation issue, and the unused clap dependency.

Document any discrepancies or new findings discovered during verification.

NO code changes. Read-only source review.
  </action>
  <verify>
    <automated>grep -rn "Command::new" /Users/kshum/Documents/gitproj/semantic-diff/src/ | wc -l</automated>
  </verify>
  <done>All research findings verified against current source; any discrepancies noted</done>
</task>

<task type="auto">
  <name>Task 2: Write comprehensive AUDIT-REPORT.md</name>
  <files>.planning/phases/04-red-team-audit/AUDIT-REPORT.md</files>
  <action>
Create `.planning/phases/04-red-team-audit/AUDIT-REPORT.md` with the following structure:

**Header section:**
- Title: "Red Team Security Audit Report -- semantic-diff v1.0"
- Date, auditor (Claude), scope, methodology
- Executive summary: total findings count by severity (CRITICAL/HIGH/MEDIUM/LOW/INFO)

**Finding format** (use for EVERY finding):
```
### FINDING-XX: [Short Title]
- **Severity:** CRITICAL / HIGH / MEDIUM / LOW / INFO
- **Attack Surface:** Command Injection / Signal-PID / LLM Trust / Path Traversal / Dependency
- **Location:** `src/file.rs:NN`
- **Description:** What the vulnerability is
- **Exploit Scenario:** How an attacker could exploit it
- **Remediation:** What Phase 5 should do to fix it
- **Requirement:** Maps to [REQ-ID]
```

**Attack Surface 1: Command Execution (CMD-03)**
Include findings for all 5 Command::new call sites from research:
- FINDING-01: LLM prompt passed as CLI argument (claude) -- HIGH
- FINDING-02: LLM prompt passed as CLI argument (copilot) -- HIGH
- FINDING-03 through 05: Git command calls -- LOW (document as audited-safe)

**Attack Surface 2: Signal & PID Handling**
Include findings from research:
- PID file in world-writable /tmp/ -- HIGH
- Predictable PID file name -- MEDIUM
- Non-atomic PID file write -- MEDIUM
- No ownership validation on PID read -- MEDIUM
- Log file in /tmp/ with predictable name -- MEDIUM

**Attack Surface 3: LLM Output Trust**
Include findings from research:
- Unbounded JSON deserialization -- MEDIUM
- No size limit on LLM response -- HIGH
- No string field length validation -- MEDIUM
- File paths from LLM not validated for traversal -- MEDIUM
- Unbounded group/change count -- LOW
- Cache deserialization unbounded -- MEDIUM

**Attack Surface 4: Path Traversal**
Include findings from research:
- Git diff paths not validated -- MEDIUM
- Binary file path extraction trusts diff -- MEDIUM
- No symlink resolution -- LOW
- Config path fallback to `.` -- LOW
- Cache path trusts git rev-parse -- LOW

**Attack Surface 5: Dependencies (DEP-01, DEP-02)**
- Parse cargo-audit-output.txt: list each advisory with severity, affected crate, and fix version
- Parse cargo-deny-output.txt: list license issues, duplicate deps, banned crates, source issues
- Include findings for: potentially unused clap dep, overly broad tokio features, unidiff supply chain concern

**Additional Findings:**
- UTF-8 truncation panic risk -- LOW
- from_utf8_lossy silent replacement -- INFO
- No exit status check on git diff -- LOW

**Summary table:** All findings sorted by severity, with columns: ID, Title, Severity, Attack Surface, Location, Requirement

**Remediation priority matrix:** Group findings by Phase 5 work area (what to fix together)

The report must be thorough enough that Phase 5 can create hardening tasks directly from it without re-reading source code.
  </action>
  <verify>
    <automated>test -f .planning/phases/04-red-team-audit/AUDIT-REPORT.md && grep -c "FINDING-" .planning/phases/04-red-team-audit/AUDIT-REPORT.md</automated>
  </verify>
  <done>AUDIT-REPORT.md exists with: (1) all Command::new calls catalogued per CMD-03, (2) cargo audit results documented per DEP-01, (3) cargo deny results documented per DEP-02, (4) all four attack surfaces covered with specific findings, (5) each finding has ID/severity/location/description/exploit/remediation, (6) summary table and remediation priority matrix included</done>
</task>

</tasks>

<verification>
- AUDIT-REPORT.md contains at least 15 findings (research identified ~20)
- Every finding has the required fields (ID, severity, attack surface, location, description, exploit scenario, remediation)
- CMD-03 is satisfied: all Command::new calls catalogued
- DEP-01 is satisfied: cargo audit results included
- DEP-02 is satisfied: cargo deny results included
- All 4 attack surfaces have dedicated sections
- Summary table lists all findings sorted by severity
</verification>

<success_criteria>
A comprehensive, structured AUDIT-REPORT.md exists that completely catalogs all security findings across command execution, signal/PID handling, LLM output trust, path traversal, and dependencies -- with severity ratings and actionable remediation recommendations that Phase 5 can directly implement from.
</success_criteria>

<output>
After completion, create `.planning/phases/04-red-team-audit/04-02-SUMMARY.md`
</output>
